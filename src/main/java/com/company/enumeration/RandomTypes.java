package com.company.enumeration;

/**
 * The enum of types for RandomType Annotation.
 */
public enum RandomTypes {
    DATE,
    DELIVERY,
    INTEGER,
    PERSON,
    PERSON_ID,
    PERSON_MIDDLE_NAME,
    PERSON_NAME,
    PERSON_SECOND_NAME,
    TEXT
}
